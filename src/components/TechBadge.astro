---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-flex items-center justify-center px-3 py-1 bg-black text-slate-300 text-xs font-mono rounded-full border border-slate-800">
  {name}
</span>
