apiVersion: v1
kind: Service
metadata:
  name: {{ include "zachara-dev.fullname" . }}
  labels:
    {{- include "zachara-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "zachara-dev.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
